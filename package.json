{
  "name": "gm-kind",
  "description": "A starter template for developing Kind contracts with AssemblyScript",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "bun-create": {
    "start": "bun run build"
  },
  "scripts": {
    "build": "kasc assembly/index.ts -o build/index.wasm -t build/index.wat"
  },
  "dependencies": {
    "@everyprotocol/kind-as": "^0.1.1"
  }
}
